# エンジニア共有ノート（タイムレコード：セルフスタート／インポート／運用）

> 2026-07-13 更新。上部固定バー（会社・店舗切替）まで反映。変更履歴＝`CHANGELOG.md`、判断経緯＝`JUDGMENT_LOG.md`。

## 0. まず読む順番（実装者向け）
1. **CHANGELOG.md** … 直近の変更を日付別に一望（最新は 2026-07-13 上部固定バー）
2. **仕様書 §2.1.1 実装早見表** … 画面ID → 本番ルート → 状態キー → 主要関数
3. **JUDGMENT_LOG.md / 仕様書 §27.1** … PdM判断（案 vs 採用）#1〜#19
4. 必要に応じて コード（index.html）／ライブモック

> 矛盾時の優先：UI/挙動は **コード(index.html) を正**、データスキーマは **CSVインポートテンプレを正**。

## 1. 直近の主要変更（2026-06-24〜07-13）
- **業態・ブランド → 業態に統合**：ブランド／ブランドコード廃止。店舗の所属先は「所属業態」。業態は**テキストで名前付け新規登録**（プルダウンでない）。（§3.4.1 / #13,#15）
- **`brand` → `businesstype` に一括改称**：本番ルート `/settings/business-types`、モック画面ID・状態キー・関数を改称（両リポ）。（§8 / #17）
- **各設定リスト全行に［編集］［削除］動線**、**登録モーダルの新規/編集 出し分け**（新規=空・登録／編集=プリフィル・更新）。（§3.4.2 / #14,#16）
- **マスタ参照の連動**：登録済み業態/店舗/雇用区分を参照プルダウンに動的反映（登録/削除で即連動）。（§3.4.3 / #18）
- **上部固定バー：会社・店舗 コンテキスト切替**（社長方針）：全画面共通・選択保持。会社→店舗の階層＋インクリメンタル検索。**会社切替は顧客・CS両方**。（§3.7 / #19）
  - **表示範囲**：運用段階の画面のみ。認証・登録・オンボーディング（ログイン／自己登録／PWリセット／招待・PW設定／登録・編集フォーム `d2`,`d1e`,`c1e-*`／初期セットアップ `b0`／setupモード設定）は非表示。
  - 実装：`buildGlobalScope` / `GLOBAL_SCOPE` / `applyGlobalScope` / `COMPANIES` / `NO_SCOPE_BAR`（`body.no-scope-bar`）。

## 2. ライブモック（main 反映済み・要ハードリフレッシュ）
- owner-mock: https://suguru789987.github.io/rakmy-owner-registration-mock/
- import-spec: https://suguru789987.github.io/rakmy-timerecord-import-spec/
- 主要ディープリンク：
  - セルフスタート起点: `.../#login` → `#a1`〜`#a4` → `#b0`
  - 業態＋編集動線＋連動: `.../#businesstype-ops-settings`
  - 運用画面で上部バー表示・店舗切替: `.../#c1` ／ 登録画面で非表示: `.../#d2`
  - 条件別メールフォーマット早見: `.../#mail-formats`
  - 新規インポート: import-spec の `#tpl-setup` / `#saas-setup`

## 3. 仕様書（読む順番つき）
https://github.com/suguru789987/rakmy-owner-registration-mock/blob/main/20260501_新規登録運用登録分離仕様.md
- §2.1.1 = 実装早見表 / §2.1 = 画面インベントリ
- §3.4.1 登録経路統一 / §3.4.2 新規・編集出し分け / §3.4.3 マスタ参照連動 / §3.7 上部固定バー
- §8 ルーティング（`/settings/business-types` ほか）
- §4.3／§4.4 = 自己登録〜運用の状態遷移／招待・PW・エラー系
- §26 = 差分サマリ / §27.1 = PdM判断記録（#1〜#19）
- ※ §1〜§11 前半は最新設計に更新済み。§12〜§23 は変更履歴（参照用）

## 4. セルフサーブ登録 検証
- 検証プラン：`20260626_01a_セルフサーブ登録_検証プラン.tsv`（4象限＋横断・39項目）
- 検証用入力値：`20260626_01b_セルフサーブ登録_検証用入力値`（md/tsv）

## 5. 残オープン項目 / 実装時に要確定
- メール本文の最終コピー（CS確定・別チケット）。条件別フォーマット／PW要否は確定済み。
- 上部固定バーの**会社切替**：閲覧スコープ切替として提供する方針だが、**実データの法人分離・所有・権限境界の扱いは実装時に別途詰める**（データ分離型マルチテナントは経営管理領域）。
- 業態の本番ルート改称に伴う既存 `brand` 系実装の置換（`/settings/business-types`）。

---
🤖 入口は本ノート／CHANGELOG.md／JUDGMENT_LOG.md。最新は常に main の各成果物を参照。
